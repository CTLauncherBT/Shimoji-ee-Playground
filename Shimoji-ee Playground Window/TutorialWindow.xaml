<Window x:Class="ShimojiPlaygroundApp.TutorialWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tutorial"
        Width="433"
        Height="199"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="#FFF3EEDC"
        Topmost="True"
        FontFamily="Consolas">

    <Window.Resources>
        <SolidColorBrush x:Key="PaperBg" Color="#FFF3EEDC"/>
        <SolidColorBrush x:Key="Ink" Color="#FF1A1A1A"/>
        <SolidColorBrush x:Key="PanelBg" Color="#FFF7F2E6"/>
        <SolidColorBrush x:Key="BorderInk" Color="#FF2A2A2A"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Ink}"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#FFE6DFCC"/>
            <Setter Property="Foreground" Value="{StaticResource Ink}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderInk}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,3"/>
        </Style>
    </Window.Resources>

    <DockPanel>

        <Border DockPanel.Dock="Top"
                Height="29"
                Background="#FFE6DFCC"
                BorderBrush="{StaticResource BorderInk}"
                BorderThickness="0,0,0,1"
                MouseLeftButtonDown="DragBar_MouseLeftButtonDown">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Tutorial"
                           VerticalAlignment="Center"
                           Margin="10,0,0,0"/>

                <Button Grid.Column="1"
                        Content="✕"
                        Width="40"
                        Height="29"
                        BorderThickness="0"
                        Click="Close_Click"/>
            </Grid>
        </Border>

        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Background="{StaticResource PanelBg}"
                    BorderBrush="{StaticResource BorderInk}"
                    BorderThickness="1"
                    CornerRadius="6"
                    Padding="12">

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock TextWrapping="Wrap">
                        <Run Text="Some people don't know how to use the Playground when it's launched (keybinds etc.)."/>
                        <LineBreak/>
                        <LineBreak/>
                        <Run Text="Do you want to see a short tutorial for the Playground?"/>
                    </TextBlock>
                </ScrollViewer>
            </Border>

            <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,12,0,0">

                <Button Content="No"
                        Width="100"
                        Margin="0,0,10,0"
                        Click="No_Click"/>

                <Button Content="Yes"
                        Width="100"
                        Click="Yes_Click"/>
            </StackPanel>
        </Grid>
    </DockPanel>
</Window>